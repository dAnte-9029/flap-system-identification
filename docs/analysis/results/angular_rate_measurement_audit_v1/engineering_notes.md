Initial pre-read path resolution stopped on duplicate Sep17 raw copies; all copies were verified against the frozen SHA256, then source-root order was used. First parse attempt stopped after nine Sep7 logs because Sep17 cached state_sample_timestamp_us is entirely absent (NaN). Raw local-position timestamp_sample exists; the audit now verifies stored timestamps wherever present, records missing counts, and uses raw timestamps for the same fixed timing sensitivity. No model metric, threshold, or alignment rule was changed. Access log retains both attempts.
