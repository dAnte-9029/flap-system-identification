# Conventions Guidelines

This directory owns foundational phase, coordinate, unit, timestamp, and sample-identity conventions. It must not contain training, label reconstruction, or learned-model logic.

Convention changes require separate authorization and must never be folded into a migration. Phase conventions must be explicit and covered by focused tests.
